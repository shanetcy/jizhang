# 记账 App 计划

## 定位
- 只有自己用的个人记账 App，iPhone 和安卓都能用
- 形式：PWA（网页 App，可“添加到主屏幕”、离线可用），以后可用 Capacitor 打包成原生 App
- 数据只存在本机（IndexedDB），支持导出/导入 JSON 备份

## 技术
- Vite + React + TypeScript
- Dexie（IndexedDB）+ dexie-react-hooks
- vite-plugin-pwa（离线 + 主屏幕）
- 图表用纯 CSS 条形图，不引入图表库

## 功能（第一版）
- 一笔账：金额、支出/收入、分类、币种、日期、备注
- 币种：EUR / MYR / USD，分开统计，不换算；记账时币种默认收起、沿用上次
- 明细：按月、按天分组，顶部各币种本月合计；点击编辑
- 记账：底部弹出面板，自制数字键盘，常用分类
- 统计：每个币种一个标签，分类条形图，可切换月份
- 设置：分类管理、导出/导入备份

## 设计
- 雾面底 #EDF0EE、墨色 #1C2627、线灰 #C9D0CD
- 币种色：EUR #2B4C9B、MYR #E0A526、USD #2E7A55（也用于收入）
- 支出用墨色不用红色；收入绿色带 “+”
- 金额字体 Bricolage Grotesque（本地打包），其余用系统字体
- 深色模式跟随系统

## 开发顺序
1. 搭项目、数据模型
2. 记账面板
3. 明细页
4. 统计页
5. 设置与备份
6. PWA 离线
7. 部署到免费静态托管，装到手机试用
